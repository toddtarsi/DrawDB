import React           from 'react';
import { Route }       from 'react-router';
import AppContainer    from 'containers';

export default (
  <Route name="app" component={AppContainer} path="/">
  </Route>
);

